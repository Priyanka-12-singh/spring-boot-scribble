A real-time Pictionary-style game where players guess words while others draw, built with Java ,Spring Boot and JPA (REST APIs) → for backend logic.

JPA/Hibernate + MySQL/Postgres → for storing game state, users, scores.

REST + JSON 

Frontend (basic):HTML canvas for drawing and JS fetch calls to REST endpoints. 

Authentication → Spring Security with JWT or OAuth (optional).
